vim.g.mapleader = " " -- Set the default leader.
require("dcr.lazy_init") -- Make sure that the lazy package manager is installed.
require("dcr.keybindings") -- Keybindings
require("dcr.globals") -- Some global configurations.
